# THIS IS DOCKER COMPOSE EXAMPLE
# Change compose projects in docker-compose.yml
# Change route in nginx.conf
# This projects run example nextjs and react-vite in same port same auth flow by keycloak
